name: PayrollManagementSystem
php: '8.4'
secured: true
aliases: {  }
services:
    mysql:
        version: 9.4.0
        port: '${DB_PORT}'
    redis:
        version: 7.4.7
        port: '${REDIS_PORT}'
    reverb:
        version: 1.x
        port: '${REVERB_PORT}'
integrations:
    forge: {  }
